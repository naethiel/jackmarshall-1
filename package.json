{
    "name": "jackmarshall",
    "private": true,
    "version": "0.0.0",
    "description": "A warmachine & hordes tournaments manager",
    "repository": "https://github.com/chibimi/jackmarshall",
    "devDependencies": {
        "bower": "^1.7.7",
        "http-server": "^0.9.0"
    },
    "scripts": {
        "postinstall": "bower install",
        "prestart": "npm install",
        "start": "http-server -a localhost -p 8000 -c-1 ./app"
    }
}
